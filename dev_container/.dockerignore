.ipynb_checkpoints
*.ipynb

# in development we include the .env file in the image in order to use it in docker-compose
# in production we must not add the .env file in the image. The env vars are read from a different source
# .env

# in development we use docker volumes to mount the code in the container
# in production we must include the ml_models folder in the image
ml_models/
